# Count Benchmark — $group + $count:{} (Category+Value Compound Index)
#
# Variant with a compound ascending index on (category, value).
# Inherits shared settings from count_group_count.yaml.

inherits: count_group_count.yaml

benchmark_name: count_group_count_category_value_index
run_label: group-count-category-value-index

workload_params:
  index_type: "category_value"
