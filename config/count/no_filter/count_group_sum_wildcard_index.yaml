# Count Benchmark — $group + $sum:1 (Wildcard Index)
#
# Variant with a wildcard index on $**.
# Imports shared settings from count_group_sum.yaml.

imports: count_group_sum.yaml

benchmark_name: count_group_sum_wildcard_index
run_label: group-sum-wildcard-index

workload_params:
  index_type: "wildcard"
