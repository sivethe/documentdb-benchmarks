# Count Benchmark — $group + $count:{}
#
# Measures aggregation counting performance using the $count:{}
# accumulator (MongoDB 5.0+) within a $group stage.
# Inherits shared settings from count_base.yaml.

inherits: ../count_base.yaml

benchmark_name: count_group_count
benchmark_module: benchmarks.count.count_group_count_benchmark
collection: count_test
run_label: group-count-noindex
