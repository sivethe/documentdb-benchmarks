# Count Benchmark — $count stage (Single Path Index)
#
# Variant with a single ascending index on `category`.
# Imports shared settings from count_stage.yaml.

imports: count_stage.yaml

benchmark_name: count_stage_single_path_index
run_label: count-stage-single-path-index

workload_params:
  indexSpec: {category: 1}
