# Count Benchmark — $count stage (Single Path Index + Category Filter)
#
# Filtered variant: counts only documents matching {category: "A"}.
# Inherits shared settings from the no-filter single path index config.

inherits: ../no_filter/count_stage_single_path_index.yaml

benchmark_name: count_stage_single_path_index_category_filter
run_label: count-stage-single-path-index-category-filter

workload_params:
  match_filter:
    category: "A"
