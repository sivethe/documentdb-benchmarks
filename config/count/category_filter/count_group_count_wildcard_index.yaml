# Count Benchmark — $group + $count:{} (Wildcard Index + Category Filter)
#
# Filtered variant: counts only documents matching {category: "A"}.
# Imports shared settings from the no-filter wildcard index config.

imports: ../no_filter/count_group_count_wildcard_index.yaml

benchmark_name: count_group_count_wildcard_index_category_filter
run_label: group-count-wildcard-index-category-filter

workload_params:
  match_filter:
    category: "A"
