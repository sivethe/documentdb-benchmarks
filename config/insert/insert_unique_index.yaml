# Insert Benchmark — Unique Ascending Index on timestamp
#
# Measures insert throughput with a unique ascending index on the timestamp field.
# This captures the overhead of duplicate-key checks during inserts.
# Imports shared settings from insert_base.yaml.

imports: insert_base.yaml

benchmark_name: insert_unique_index
benchmark_module: benchmarks.insert.insert_unique_index_benchmark
collection: insert_test_unique_idx
run_label: unique-index
