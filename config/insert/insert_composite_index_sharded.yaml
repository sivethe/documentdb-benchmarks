# Insert Benchmark — Composite Index (Sharded)
#
# Sharded variant: imports from insert_composite_index.yaml and enables sharding.

imports: insert_composite_index.yaml

benchmark_name: insert_composite_index_sharded
run_label: composite-index-sharded

workload_params:
  sharded: true
