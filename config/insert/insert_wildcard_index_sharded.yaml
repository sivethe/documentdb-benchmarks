# Insert Benchmark — Wildcard Index (Sharded)
#
# Sharded variant: imports from insert_wildcard_index.yaml and enables sharding.

imports: insert_wildcard_index.yaml

benchmark_name: insert_wildcard_index_sharded
run_label: wildcard-index-sharded

workload_params:
  sharded: true
