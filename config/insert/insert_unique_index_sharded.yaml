# Insert Benchmark — Unique Ascending Index (Sharded)
#
# Sharded variant: imports from insert_unique_index.yaml and enables sharding.

imports: insert_unique_index.yaml

benchmark_name: insert_unique_index_sharded
run_label: unique-index-sharded

workload_params:
  sharded: true
