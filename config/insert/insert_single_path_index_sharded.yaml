# Insert Benchmark — Single-Path Ascending Index (Sharded)
#
# Sharded variant: inherits from insert_single_path_index.yaml and enables sharding.

inherits: insert_single_path_index.yaml

benchmark_name: insert_single_path_index_sharded
run_label: single-path-index-sharded

workload_params:
  sharded: true
